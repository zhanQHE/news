# 维护记录

这份记录用于保存日常检查项，方便后续更新时快速确认项目状态。

## 日常检查

- README 中的部署入口和脚本名称保持一致。
- 检查 docs 页面是否还能正常打开。
- 运行前确认 config 目录中的示例配置仍然可读。
- 更新依赖后同步查看 requirements.txt 与 pyproject.toml。

## 发布前

- 确认本地启动脚本可以正常执行。
- 检查输出目录中是否混入临时文件。
- 如果改动了 MCP 相关内容，同步查看 FAQ 文档。
